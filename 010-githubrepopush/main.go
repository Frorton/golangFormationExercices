package main

func main() {

}

/*
Push the code to your github repo :

git add --all
git commit -m "description de ce qui est commited à github"
git push
*optionnel*
git tag (énonce l'ensemble des versions créées sur le repo)
git tag Vx.x'.x" (x = chgt majeur, x' = chgt mineur, x" = patch)
*/
